# User Story

As a project manager,
I want to create and list TODO tasks,
so that the team can track the tasks' execution status.

## Acceptance Criteria
- Create task with title and priority (`low`, `medium`, `high`).
- List all tasks sorted by creation date, in descending order.
- Mark a task as done.
- Prevent empty titles.
- Return simple error messages for invalid input.
